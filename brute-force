from string import ascii_lowercase, digits

for a in ascii_lowercase and digits:
    for b in ascii_lowercase and digits:
        for c in ascii_lowercase and digits:
            for d in ascii_lowercase and digits:
                for e in ascii_lowercase and digits:
                    for f in ascii_lowercase and digits:
                        print(a + b + c + d + e + f)